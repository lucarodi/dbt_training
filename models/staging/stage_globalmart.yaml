version: 2


models:
  - name: stg_orders
    description: one unique order per row
    columns:
      - name: orderid
        description: the primary key for stage orders
      - name: shipmode
        description: '{{doc("shipmode")}}'
